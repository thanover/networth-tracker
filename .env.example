MONGODB_URI=mongodb://mongo:27017/networth
JWT_SECRET=change-me-to-a-random-string

# Set to "true" to automatically create a demo user with sample accounts on
# first startup. The seed is idempotent — if the demo user already exists it
# is skipped. Useful for fresh deployments, PR preview environments, and demos.
# SEED_DEMO_DATA=true
# SEED_DEMO_USERNAME=demo
# SEED_DEMO_PASSWORD=demo1234
